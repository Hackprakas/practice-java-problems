import java.util.*;
import java.util.stream.Collectors;
import java.util.stream.IntStream;
public class stream {
    public static void main(String[] args) {
        // List<Integer> s=IntStream.range(0, 10).boxed().map(i->(int)Math.pow(i,2)).filter(i->i%2==0).collect(Collectors.toList());
        
        List<String> s=Arrays.asList("hey","hi","hate");
        
    }
}
